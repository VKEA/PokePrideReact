import React from 'react';

class RandomButton extends React.Component {
    render() {
        return (
            <button>
                Random
            </button>
        );
    }
}

export default RandomButton;